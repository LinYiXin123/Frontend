# Frontend 协作规则

## 项目组织

- 先阅读根目录 README 和目标应用的说明文档。
- 每个应用放在 `apps/<project-name>/`，各自维护依赖、构建、测试和文档。
- 不提交依赖、构建产物、本地缓存、真实环境变量或密钥。

## 中文提交与自动推送

- 用户确认的 GitHub 账号为 `LinYiXin123`（林艺新）。核对连接器身份；若与用户账号不匹配，不使用该连接器写入。本仓库可使用本机 Git 推送。

- 用户已授权：每次完成优化、修复或相关文档维护，完成适当验证后自动提交并推送到 `https://github.com/LinYiXin123/Frontend`，不必再次询问是否推送。
- 提交标题使用“中文类型：具体变化”，类型包括新增、优化、修复、文档、测试、重构、维护、性能、样式、构建、回退。不使用 feat、fix、docs 等英文前缀。
- 标题、提交正文、PR 描述和完成说明用简体中文，面向非技术用户，说明具体问题和可感知的变化；必要的代码名与路径保留原文。
- 提交正文说明改了什么、用户能看到的变化以及验证结果。例如：

```text
修复：解决首页卡片在窄屏下被遮挡的问题

改了什么：调整卡片间距和导航栏下方的留白。
用户可见变化：在较窄窗口中也能完整看到卡片标题和按钮。
验证结果：已检查桌面与窄屏显示，相关检查通过。
```

- 示例不代表实际验证结果；每次如实填写已执行的检查，未完成的检查明确说明。
- 提交前检查远程、当前分支和差异；只暂存本次任务相关文件，不混入其他会话或用户的未完成改动。共享目录中不要使用无差别的 `git add .`。
- 推送当前工作分支；遵守分支保护，不强制推送，不覆盖他人提交。发生冲突、认证失败或检查失败时说明实际阻碍。
- 完成后提供中文变化说明、验证结果、推送状态和 GitHub 提交链接。只读任务不创建空提交。
