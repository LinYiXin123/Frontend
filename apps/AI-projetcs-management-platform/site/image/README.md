把导航 PNG 放在这个目录下，前端会优先读取这些文件名：

- 全局看板.png
- 项目中心.png
- 预警中心.png
- 交付自检.png
- 数据与规则.png
- AI_Blob.png

如果某个 PNG 暂时不存在，导航会自动使用文字图标兜底，不会影响页面运行。

`nav/` 目录是导航专用透明底版本，由上面的原始 PNG 抠掉边缘白底生成；前端导航实际读取 `nav/*.png`。
